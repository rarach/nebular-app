language: node_js
node_js:
  - "10"
sudo: required
dist: trusty

#Install headless Chrome
addons:
  chrome: stable

cache:
  npm: false

branches:
  only:
  - master

script:
  - npm test
  - npm run e2e -- --protractor-config=e2e/protractor-ci.conf.js

after_success:
  - npm run coveralls
